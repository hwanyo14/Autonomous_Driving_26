# 평가 결과 정리 — 아키텍처 비교 · 운영점 · TRAJ_CUT

최종 갱신 2026-08-03. 대상은 query 기반 3D occupancy forecasting eval(`EOCF_EVAL_MODE=1`)이며,
데이터셋은 nuScenes `fixed_val` 5119 샘플 고정이다.

---

## 1. 실험 대상

세 개의 config 를 비교했다. **셋은 서로 하나의 축씩만 다르다.**

| 별칭 | config | 차이 | 학습 상태 |
|---|---|---|---|
| `dim96` | `..._all_filter` | 기준 (bev_feat_dim=96, 입력 896×1600) | **ep20 에서 종료** (07-30) |
| `feat128` | `..._all_filter_feat128` | bev_feat_dim 96 → **128** | ep17 까지, 계속 생성 중 (08-03) |
| `res704` | `..._all_filter_feat128_res704` | feat128 기준 입력 896×1600 → **256×704** | ep13 (08-03) |

⚠ `res704` 는 feat128 대비 **변수가 2개**다. 해상도 외에 `query_attn_sigma_match_loss_weight`
가 0.25 → 0.0 으로 꺼져 있다(저해상도에서 σ 회귀 타깃이 불안정해 의도적으로 제외). 또한 단순
downscale 이 아니라 crop 이 함께 바뀌어 **세로 FOV 유지율이 99.6% → 64.6%** 로 상단 약 35%가
잘린다(BEVDet 256×704 표준 레시피). 학습도 같은 조건이라 평가 편향은 아니지만, 성능차를
"해상도 탓"으로만 귀속하면 안 된다.

## 2. 평가 프로토콜

**지표 우선순위** (사용자 확정)

1. `IoU3d(asset)_future`
2. `3D_avg = (future × 4 + present) / 5` — 미래 4프레임 + 현재 1프레임 가중평균
3. `IoU3d(asset)_present`
4. 2D 계열은 참고용

**샘플 수**: 스크리닝 800, 확정 5119.
⚠ 800 은 5119 대비 **약 +0.0023 낙관 편향**이 있으나 조합 간 *순위*는 보존된다.
절대값 기준(예: 컷오프)을 5119 에서 가져와 800 에 적용할 때 이 편향을 감안할 것.
⚠ 800 은 *밴드*는 찾아주지만 *봉우리*는 못 찾는다(컷 비율 최적이 800 에서 20%, 5119 에서 10%였음).

**부분 진행 비교**: eval 순서가 deterministic 이라 같은 N 은 같은 샘플이다.
진행률이 다른 두 런의 마지막 줄을 그냥 비교하면 안 되고, **공통 N 에서 맞춰야** 한다.

---

## 3. TRAJ_CUT (`EOCF_EVAL_FG_TRAJ_ACC_MAX` / `EOCF_EVAL_TRAJ_CUT_SCOPE`)

과거 3프레임 중심좌표의 2차 차분 `traj_acc = |c₂ − 2c₁ + c₀|` 이 임계값을 넘는 query 를 제거한다.
물리적으로 `a = acc/Δt²`, Δt=0.5s 이므로 **5.0 m ⟺ 20 m/s² ≈ 2g** 초과 가속을 배제하는 것과 같다.

### 3.1 임계값 스윕 (dim96 ep18 @5119, fg0.9/occ0.75)

| acc_max | future | present | 3D_avg |
|---|---|---|---|
| 0 (OFF) | 0.1412 | 0.1790 | 0.14876 |
| 3.75 | **0.1439** | 0.1790 | **0.15092** |
| **5.0 (기본값)** | **0.1439** | 0.1790 | **0.15092** |
| 6.0 | 0.1435 | 0.1789 | 0.15058 |

**3.75 와 5.0 이 완전 동률**이고 6.0 부터 하락 → 5.0 은 평평한 봉우리의 중앙이다.
미세조정 여지 없음. acc < 3.75 구간은 미측정(컷 비율이 이미 ~10%라 더 내리면 TP 손실 우려).

### 3.2 scope=future 의 효과

`scope=all` 은 query 를 통째로 제거해 present 도 같이 깎인다. `scope=future` 는 미래 프레임
렌더링에만 컷을 적용해 **present 를 정확히 보존**한다.

| dim96 ep18 @5119 | future | present |
|---|---|---|
| acc5.0 `scope=all` | 0.1439 | 0.1787 |
| acc5.0 `scope=future` | 0.1439 | **0.1790** (= OFF 와 동일) |

→ **기본값 = `acc_max 5.0` + `scope=future`.**

### 3.3 이득의 일관성

| 체크포인트 | OFF → ON (future) | 상승률 |
|---|---|---|
| dim96 ep18 @5119 | 0.1412 → 0.1439 | +1.9% |
| dim96 ep19 @5119 | 0.1370 → 0.1395 | +1.8% |
| dim96 ep20 @5119 | 0.1406 → 0.1432 | +1.8% |
| feat128 ep14 @800 | 0.1424 → 0.1440 | +1.1% |
| feat128 ep15 @800 | 0.1418 → 0.1434 | +1.1% |

present 손실은 **모든 경우 정확히 0**. dim96 은 +1.8~1.9%, feat128 은 +1.1% 로 구조에 따라
폭이 다르지만 부호는 항상 양수다. 후처리 고정 이득이라 **조합 간·구조 간 순위를 바꾸지 않는다**
→ 비교 실험은 CUT OFF 로 하고, 최종 수치만 CUT ON 으로 뽑으면 된다.

⚠ **오라클(`_oracle_on`)에는 컷을 적용하지 않는다.** `scope=future` 는 오라클 override 이후에
적용되므로 코드에서 `and not _oracle_on` 으로 명시 차단되어 있다. `eval_oracle.sh` 에는 관련
인자가 아예 없다.

---

## 4. 운영점 (fg_thr / occ_thr)

### 4.1 구조별 최적 운영점

| 구조 | 최적 fg | 최적 occ | occ 민감도 |
|---|---|---|---|
| dim96 | **0.90** | 0.75 (무영향) | 무영향 |
| feat128 | **0.85** | **0.85** | **유의** |
| res704 | **0.90** | 0.75 (무영향) | 무영향 |

**occ 가 언제 일하는가**: fg0.90 에서는 선택된 query 의 cls 가 전부 0.9~1.0 이라 occ 가 아무
일도 하지 않는다. **fg 를 낮춰야 경계선 query 가 들어오고 그때부터 occ 가 작동한다.**
feat128 만 최적 fg 가 0.85 라서 occ 가 유의미해진 것이다.

### 4.2 feat128 ep15 격자 (@800, CUT ON)

fg 축:

| fg (occ0.75 고정) | future | 3D_avg |
|---|---|---|
| 0.80 | 0.1386 | 0.14556 |
| **0.85** | **0.1443** | **0.15150** |
| 0.90 | 0.1434 | 0.14972 |

occ 축 (fg0.85 고정) — **단조 상승 후 0.85 에서 봉우리**:

| occ | future | present | 3D_avg |
|---|---|---|---|
| 0.70 | 0.1436 | 0.1789 | 0.15066 |
| 0.75 | 0.1443 | 0.1803 | 0.15150 |
| 0.80 | 0.1448 | 0.1816 | 0.15216 |
| **0.85** | **0.1451** | 0.1825 | **0.15258** |
| 0.90 | 0.1449 | **0.1830** | 0.15252 |
| 0.95 | 0.1436 | 0.1820 | 0.15128 |

### 4.3 res704 ep13 격자 (@800, CUT OFF) — 전량

| fg / occ | future | present | 3D_avg | 2D_fut |
|---|---|---|---|---|
| **0.90 / 0.75** | **0.1319** | 0.1675 | **0.13902** | 0.1914 |
| 0.90 / 0.85 | 0.1316 | 0.1679 | 0.13886 | 0.1908 |
| 0.85 / 0.85 | 0.1293 | 0.1673 | 0.13690 | 0.1772 |
| 0.85 / 0.75 | 0.1281 | 0.1649 | 0.13546 | 0.1756 |
| 0.80 / 0.85 | 0.1195 | 0.1564 | 0.12688 | 0.1597 |
| 0.80 / 0.75 | 0.1175 | 0.1530 | 0.12460 | 0.1573 |

fg 단조 감소이며 아래로 갈수록 가팔라진다: 0.90→0.85 −1.5%, 0.85→0.80 **−9.0%**.
FP 가 원인이다 — fg0.80 에서 TP 는 +30% 느는데 **FP 가 5.23e7 → 1.10e8 (+110%)** 로 두 배 이상 는다.
2D 가 3D 보다 더 크게 무너지는 것(fg0.90→0.80 에서 3D_fut −11% vs 2D_fut −18%)은,
들어오는 query 가 z 축이 아니라 **BEV 평면 위치부터 틀렸다**는 뜻이다.

### 4.4 fg0.95 는 전 구조 공통 붕괴

feat128 ep14 @800 에서 fg0.95 는 3D_avg 0.1130~0.1147 로 fg0.90(0.1506) 대비 **−0.036**.
dim96 에서도 같은 규모로 무너졌다. **fg0.95 는 어느 격자에서도 열 필요 없다.**

### 4.5 ⚠ 운영점은 에폭마다 이동한다

| 체크포인트 | 최적 조합 | @5119 3D_avg |
|---|---|---|
| feat128 ep14 | fg0.90 / occ0.75 | 0.14716 |
| feat128 ep15 | fg0.85 / occ0.85 | **0.15064** |

**고정점 fg0.9/occ0.75 로만 비교하면 ep14(0.14716) > ep15(0.14666) 로 ep14 가 이긴다.**
각자 최적점에서 비교하면 ep15 가 +0.00348 로 뒤집힌다.
→ **에폭 순위를 고정 운영점으로 판정하면 틀린다.**

---

## 5. 에폭 곡선

### 5.1 dim96 @5119 (fg0.9/occ0.75, CUT ON `scope=future`)

| epoch | future | present | 3D_avg |
|---|---|---|---|
| **18** | **0.1439** | **0.1790** | **0.15092** |
| 19 | 0.1395 | 0.1755 | 0.14670 |
| 20 | 0.1432 | 0.1783 | 0.15022 |

⚠ ep15/16/17 은 @5119 로 평가된 적이 없다. **"ep18 이 봉우리"는 {18,19,20} 중 최고라는 뜻**이다.

### 5.2 feat128 @5119 (fg0.9/occ0.75, CUT ON)

| epoch | future | present | 3D_avg |
|---|---|---|---|
| 12 | 0.1335 | 0.1646 | 0.13972 |
| 13 | 0.1378 | 0.1727 | 0.14478 |
| 14 | 0.1407 | 0.1730 | 0.14716 |
| 15 | 0.1403 | 0.1721 | 0.14666 |
| **15 (최적점 fg0.85/occ0.85)** | **0.1432** | **0.1804** | **0.15064** |

**ep16 / ep17 은 미완**이다. ep16 은 두 운영점 모두에서 25% 지점까지 진행 후 중단됐고,
그 구간에서 ep15 대비 **−0.0025 로 6개 측정점 전부 안정적으로 낮았다**. 다만 ep16 의 임계값을
재조정하지 않은 상태라 4.5절의 함정일 가능성이 남아 있다. ep17 은 사실상 미측정.

→ **feat128 의 봉우리는 확정되지 않았다.** ep18/19/20 체크포인트가 나오면 재판정이 필요하다.

---

## 6. 아키텍처 비교

### 6.1 같은 에폭 (@800, fg0.9/occ0.75, **CUT OFF** = 순수 포워드)

| ep15 | 3D_fut | 3D_pres | 3D_avg | 2D_fut | 2D_pres |
|---|---|---|---|---|---|
| dim96 | 0.1363 | 0.1712 | 0.14328 | 0.1981 | 0.2290 |
| **feat128** | **0.1418** | **0.1750** | **0.14844** | **0.2070** | **0.2345** |
| Δ | **+0.0055** | +0.0038 | +0.00516 | +0.0089 | +0.0055 |

**5개 지표 전부 feat128 우세.** 부분 진행 전 구간에서 부호가 한 번도 뒤집히지 않았다.
후처리(TRAJ_CUT) 를 끈 상태이므로 모델 자체 성능 차이다.

### 6.2 각자 최적점 · 전체 샘플 (@5119, CUT ON) — 유일한 완결 정면 대결

| | epoch | 운영점 | 3D_fut | 3D_pres | 3D_avg |
|---|---|---|---|---|---|
| dim96 | 18 | fg0.90/occ0.75 | **0.1439** | 0.1790 | **0.15092** |
| feat128 | 15 | fg0.85/occ0.85 | 0.1432 | **0.1804** | 0.15064 |
| Δ (f128−dim96) | | | −0.0007 | +0.0014 | −0.00028 |

**사실상 무승부**(avg 차이 0.00028). 다만 **dim96 은 ep18, feat128 은 ep15** 로,
feat128 이 3에폭을 덜 받은 상태에서 낸 수치다.

### 6.3 해상도 (@800, ep13, 각자 최적점, CUT OFF)

| | 3D_fut | 3D_pres | 3D_avg | 2D_fut | GPU |
|---|---|---|---|---|---|
| feat128 (896×1600) | **0.1405** | **0.1772** | **0.14784** | **0.2005** | ~23 GB |
| res704 (256×704) | 0.1319 | 0.1675 | 0.13902 | 0.1914 | **~8 GB** |
| Δ | −0.0086 (−6.1%) | −0.0097 (−5.5%) | −0.00882 (−6.0%) | −0.0091 | **−65%** |

입력 픽셀 1/8 (KV 토큰 44,100 → 5,544) + 세로 FOV 상단 35% 손실의 대가가 **약 −6%**.
5개 지표가 고르게 내려가며 특정 축만 붕괴하지 않는다.
res704 @800 소요는 약 18분(feat128 ~25분)이나, 다른 런과 겹친 구간이 있어 **정확한 FPS 는 아니다** —
필요하면 단독 실행으로 재측정할 것.

### 6.4 현재 최고 기록

| 지표 | 최고 | 출처 |
|---|---|---|
| IoU3d_future | **0.1439** | dim96 ep18 @5119, fg0.9/occ0.75, CUT ON |
| 3D_avg | **0.15092** | 동일 |
| IoU3d_present | 0.1804 (자체 최고) | feat128 ep15 @5119, fg0.85/occ0.85 |

⚠ present 는 별도로 **0.1842** 기록이 존재한다고 보고됐으나, 본 문서의 어느 런에서도 재현되지
않았다(자체 최고 0.1804). **출처와 조건 미확인.**

---

## 7. 열린 항목

1. **feat128 ep18/19/20** — 체크포인트 도착 시 에폭 스크리닝. 6.2 의 무승부를 가르는 핵심.
   권장 순서: ① 에폭당 `fg0.85/occ0.85 @800 CUT ON` 1런으로 곡선 방향 확인 →
   ② 상위 1~2 에폭만 `fg{0.85,0.90} × occ{0.75,0.85}` 4조합 → ③ 최종 조합 @5119 + VIS.
   전수 격자(16런 5h) 대신 이 방식이면 12런 2.5h.
2. **CUT 이득의 임계값 독립성 검증** — 지금까지 CUT 이득은 에폭이 달라도 정확히 +0.0016
   (fg0.9/occ0.75)이었다. **임계값에도 무관하면 격자를 CUT ON 으로 한 번만 펴면 된다.**
   `feat128 ep15 fg0.85/occ0.85 CUT OFF` **1런**이면 확정된다(기존 CUT ON 0.1451 과 대조).
   이 런은 논문 ablation(최종 운영점에서의 CUT 효과)으로도 필요하다.
3. **dim96 `fg0.85 × occ≥0.85`** — 미측정 칸. dim96 의 "occ 무영향" 결론은 occ∈{0.70,0.75,0.80}
   범위에서만 확인됐고, feat128 이 이긴 지점이 정확히 `fg0.85 × occ0.85` 다. @800 2런.
4. **res704 CUT ON** — res704 의 TRAJ_CUT 이득은 미측정. 해상도가 낮아 궤적이 거칠면 이득이
   더 클 수 있다. 최적점 fg0.9/occ0.75 에서 1런.
5. **present 0.1842 출처 확인.**

---

## 8. 재현 방법

### 8.1 환경 변수

```bash
export EOCF_EVAL_MODE=1
export EOCF_EVAL_FG_THR=0.9            # 구조별 최적: dim96/res704 0.90, feat128 0.85
export EOCF_EVAL_OCC_THR=0.75          # feat128 은 0.85
export EOCF_EVAL_MAX_SAMPLES=0         # 0 = 5119 전체, 800 = 스크리닝
export EOCF_EVAL_FG_TRAJ_ACC_MAX=5.0   # 0 = TRAJ_CUT 끔
export EOCF_EVAL_TRAJ_CUT_SCOPE=future # future 권장 / all 은 present 손실
export EOCF_EVAL_NMS_RADIUS=0
export EOCF_EVAL_WEIGHT_MODE=ones
export EOCF_EVAL_METRIC_EVERY=48
export EOCF_EVAL_VIS=0                 # 1 이면 GPU 메모리 약 2배
```

`eval_total.sh` 가 위 값을 담고 있다. 오라클은 `eval_oracle.sh`(TRAJ_CUT 인자 없음).

### 8.2 자원 한계 (24코어 / 98GB GPU / 62GB RAM 기준)

| 항목 | 값 |
|---|---|
| 동시 실행 상한 | **2개** (하드 캡) |
| 병목 | **CPU** — eval 은 GPU 4~50%, run 당 약 6.9 코어 |
| GPU 메모리 (VIS off) | dim96 ~19GB / feat128 ~23GB / res704 ~8GB |
| GPU 메모리 (VIS on) | **약 2배** — dim96 35GB, feat128 **42.8GB** |

🔴 **2026-08-02 14:01 실제 사고**: feat128 + VIS=1 을 2개 동시 실행(GPU 85.6GB, 87%) 하다가
NVRM GSP RPC failure → FULLCHIP_RESET → `CUDA error: unspecified launch failure` → **시스템 재부팅**.
CPU/RAM 문제가 아니라 GPU 결함이었다. **대량 런은 전부 VIS=0 으로 돌리고 시각화는 최종 승자 1개만
따로 켤 것.** metric 에는 영향 없다.

### 8.3 스크립트 (scratchpad)

`eval_run.sh` — config 파라미터화된 단일 run 러너.

```bash
CFG=<config이름> EP=15 ACC=0 MAXS=800 FG=0.9 OCC=0.75 VIS=0 eval_run.sh <PORT>
```

⚠ 작성 시 지킬 것 (전부 실제로 겪은 사고):

- **`set -e` 금지** — 실패해도 `_STATUS`/DONE 마커를 남겨야 '죽은 run'과 '진행 중 run'을 구분한다.
- **결과 `.out` 은 프로젝트 안에도 복사** — `/tmp` 는 재부팅으로 소실된다(2026-08-02).
- **워커 수는 `python -u tools/test.py` 로만 센다** — 부모(`torch.distributed.run`)까지 세면
  1 run 이 2로 잡혀 큐가 영구 대기한다.
- **`setsid` 로 띄운다** — `nohup ... &` 만으로는 부모 셸이 죽을 때 프로세스 그룹째 kill 된다(2026-08-03).
- **큐는 "프로세스 사망"이 아니라 "슬롯 확보"를 기다린다** — 전자로 짜면 슬롯을 놀린다.
- **TAG 에 모든 변수를 넣는다** — scope 를 빼먹어 `scope=all` 결과가 `scope=future` 로 덮인 적 있다.

### 8.4 결과 보관 위치

```
work_dirs/<config>/eval/_sweep_results/*.out    # 파일명에 ep/fg/occ/acc/n 전부 포함
logs/<config>/eval/<timestamp>.log              # 진행 중 per-N 지표 (부분 비교용)
```
